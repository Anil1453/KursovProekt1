@model ControlPanel.Models.Room

@{
    ViewData["Title"] = "Детайли за стая";
}

<h1>Детайли за стая</h1>

<div class="card">
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">Име на стаята</dt>
            <dd class="col-sm-9">@Model.RoomName</dd>

            <dt class="col-sm-3">Код</dt>
            <dd class="col-sm-9">@Model.RoomCode</dd>

            <dt class="col-sm-3">Описание</dt>
            <dd class="col-sm-9">@Model.Description</dd>

            <dt class="col-sm-3">Ниво на достъп</dt>
            <dd class="col-sm-9">@Model.AccessLevel</dd>

            <dt class="col-sm-3">Активна</dt>
            <dd class="col-sm-9">
                @if (Model.IsActive)
                {
                    <span class="badge bg-success">Да</span>
                }
                else
                {
                    <span class="badge bg-danger">Не</span>
                }
            </dd>
        </dl>
    </div>
</div>

<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">✏️ Редактирай</a>
    <a asp-action="Index" class="btn btn-secondary">← Назад към списъка</a>
</div>